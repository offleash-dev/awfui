TEST
dialog variants

ADD
text size to theme (large, medium, small to be calculated by hw interface?)  or a fixed size?
change theme variables to be more self documenting
	-eg , dialogBackgroundColor, dialogBorderColor, widgetBackgroumdColor, widgetBorderColor, widgetTextColor, enabled/disabled versions of each


polish for AFScreen
optional refinements you might want later:
    invalidate/dirty‑rect redraw
    partial redraw optimization
    scrollable containers
    focus model (for keyboard/rotary input)


layout support via build script (see thoughts doc)